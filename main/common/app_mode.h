#pragma once

// Keep 0 for real BluFi provisioning and time synchronization; set 1 only for the local demo path.
#define ECLOCK_DEMO_MODE 0

// Temporary hand-motion preview. Keep 0 for the normal clock movement schedule.
#define ECLOCK_EFFECT_PREVIEW 0
